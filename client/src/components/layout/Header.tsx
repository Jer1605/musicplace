import React from "react";

type HeaderProps = {
    title: string,
    paragraph: string,
}

export const Header: React.FunctionComponent<HeaderProps> = ({title, paragraph}) => {
    return <div>sd</div>
}